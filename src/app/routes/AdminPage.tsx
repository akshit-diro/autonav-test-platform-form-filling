/**
 * Placeholder page for scenario-protected route.
 * Only users with allowedScenarios including "admin" can access.
 */
export function AdminPage() {
  return (
    <div>
      <h1>Admin</h1>
    </div>
  )
}
