/**
 * Declare jquery-ui-dist so the side-effect import type-checks.
 * jQuery UI datepicker typings come from @types/jqueryui (augments JQuery).
 */
declare module 'jquery-ui-dist/jquery-ui' {
  // Side-effect only: augments jQuery with datepicker
}
